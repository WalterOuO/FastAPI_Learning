name: FastAPI CI Test

on:
  push:
    branches: [ "main" ]

jobs:
  build-backend:
    environment: testing

    env:
      # 常數、非機密資訊可以直接寫明文
      DATABASE_HOSTNAME: localhost
      DATABASE_PORT: 5432
      DATABASE_NAME: fastapi
      DATABASE_USERNAME: postgres
      ALGORITHM: HS256
      ACCESS_TOKEN_EXPIRE_MINUTES: 60
      
      # 關鍵：敏感機密資訊，100% 透過安全憑證代入
      DATABASE_PASSWORD: ${{ secrets.DATABASE_PASSWORD }}
      SECRET_KEY: ${{ secrets.SECRET_KEY }}

    services:
      postgres:
        image: postgres
        env:
          POSTGRES_PASSWORD: ${{ secrets.DATABASE_PASSWORD }}
          POSTGRES_DB: fastapi_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U Postgres -d fastapi_test" 
          --health-interval=10s 
          --health-timeout=5s 
          --health-retries=5


    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install python version 3.10
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Update pip
        run: python -m pip install --upgrade pip

      - name: Install all dependencies
        run: pip install -r requirements.txt

      - name: Test with pytest
        run: |
          pip install pytest
          pytest


      - name: Log in to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

      - name: Set up Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@v2


      - name: Build and push
        id: docker_build
        uses: docker/build-push-action@v3
        with:
          context: ./fastapi
          file: ./fastapi/Dockerfile
          # builder: ${{ steps.buildx.outputs.n ame }}
          push: true
          tags: ${{ secrets.DOCKER_HUB_USERNAME }}/fastapi:latest
          cache-from: type=local,src=/tmp/.buildx-cache
          cache-to: type=local,dest=/tmp/.buildx-cache

      - name: Image digest
        run: echo ${{ steps.docker_build.outputs.digest }}


  # 部屬到 Ubuntu Server 就這麼做

  # deploy-backend: 
  #   runs-on: ubuntu-latest
  #   needs: [build-backend]
  #   environment: production

  #   steps:
  #     - name: Checkout code         # pull the latest code from github to get the deployment scripts
  #       uses: actions/checkout@v4

  #     - name: Deploy to Server via SSH
  #       run: |
  #         ssh -o StrictHostKeyChecking=no ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} "
  #           cd /app/src && 
  #           git pull && 
  #           docker compose -f docker-compose.prod.yml pull && 
  #           docker compose -f docker-compose.prod.yml up -d
  #         "
